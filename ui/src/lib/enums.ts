export enum MAP_INFO_EDITORS {
  TITLE = "title",
}

export enum API_PATHS {
  SIGN_IN = "/auth/signin",
  SIGN_UP = "/auth/signup",
  CHECK_AUTH = "/auth/check",
  NEW_MAP = "/maps",
  GET_MAP = "/maps",
  PATCH_MAP = "/maps",
}
